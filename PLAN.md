# Clean up Home date card and Documents status chips

**Home tab — date card**
- Remove the small "Needs attention / Building momentum / On track" pill at the top right of the big day-count card. The card will just show the greeting, name, and days countdown with branch.

**Documents tab — status options**
- Today each document shows 4 chips (Needed, Requested, Received, Verified) which wrap awkwardly and split words.
- Replace the row of chips with a single, clean status selector: a tap-to-open menu (dropdown) that shows the current status with its color, and lets you pick from the four options. No more wrapped text or ellipses, and it looks tidier on every screen size.

Everything else on these screens stays the same.